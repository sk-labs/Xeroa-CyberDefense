<!--
  XEROA CYBERDEFENSE
  @author Shakir ullah - @sk-labs
  @description Security rate limit error page - Cybersecurity themed
  @version 1.0.0
-->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>Security Alert - Rate Limit Exceeded | Xeroa CyberDefense</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="Xeroa CyberDefense - Enterprise-Grade Security System" name="description" />
    <meta content="Shakir ullah (@sk-labs)" name="author" />

    <!-- Bootstrap Css (CDN fallback if not available locally) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Remix Icons (CDN fallback) -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .security-alert-container {
            max-width: 700px;
            width: 95%;
            padding: 15px;
            margin: auto;
        }

        .security-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            padding: 1.5rem 2rem;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: slideUp 0.5s ease-out;
        }

        @media (max-width: 768px) {
            .security-card {
                padding: 1.5rem 1.25rem;
            }
        }

        @media (min-width: 1024px) {
            .security-alert-container {
                max-width: 750px;
            }
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .shield-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 0.75rem;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 10px 30px rgba(245, 87, 108, 0.4);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                box-shadow: 0 10px 30px rgba(245, 87, 108, 0.4);
            }
            50% {
                transform: scale(1.05);
                box-shadow: 0 15px 40px rgba(245, 87, 108, 0.6);
            }
        }

        .shield-icon i {
            font-size: 2.5rem;
            color: white;
        }

        .security-title {
            font-size: 1.35rem;
            font-weight: 700;
            color: #1a1a2e;
            margin-bottom: 0.4rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .security-message {
            font-size: 0.95rem;
            color: #495057;
            margin-bottom: 0.75rem;
            line-height: 1.5;
        }

        .countdown-timer {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 0.75rem 1rem;
            border-radius: 12px;
            margin: 1rem 0;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        .countdown-label {
            font-size: 0.75rem;
            opacity: 0.9;
            margin-bottom: 0.25rem;
        }

        .countdown-time {
            font-size: 1.75rem;
            font-weight: 700;
            font-family: 'Courier New', monospace;
            letter-spacing: 2px;
        }

        @media (max-width: 480px) {
            .countdown-time {
                font-size: 1.5rem;
            }
        }

        .security-actions {
            display: flex;
            gap: 0.75rem;
            justify-content: center;
            flex-wrap: wrap;
            margin: 1rem 0 0.75rem 0;
        }

        .btn-security {
            padding: 0.65rem 1.75rem;
            border-radius: 10px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            font-size: 0.95rem;
        }

        @media (max-width: 480px) {
            .btn-security {
                padding: 0.6rem 1.5rem;
                font-size: 0.9rem;
            }
        }

        .btn-primary-security {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-primary-security:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.6);
            color: white;
        }

        .btn-secondary-security {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .btn-secondary-security:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

        .developer-credit {
            margin-top: 0.75rem;
            padding: 0.85rem;
            background: linear-gradient(135deg, #667eea15 0%, #764ba215 100%);
            border-radius: 12px;
            font-size: 0.8rem;
            line-height: 1.6;
            border: 1px solid rgba(102, 126, 234, 0.2);
        }

        .developer-credit-title {
            font-weight: 700;
            color: #667eea;
            margin-bottom: 0.4rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            font-size: 0.9rem;
        }

        .developer-credit-title i {
            color: #f5576c;
            animation: pulse 2s infinite;
        }

        .developer-credit-text {
            color: #495057;
            text-align: center;
        }

        .developer-link {
            color: #667eea;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .developer-link:hover {
            color: #764ba2;
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="security-alert-container">
        <div class="security-card">
            <div class="shield-icon">
                <i class="ri-shield-cross-line"></i>
            </div>

            <h1 class="security-title">🛡️ Security Alert</h1>

            <p class="security-message">
                <strong>Access Temporarily Restricted</strong><br>
                Too many requests detected. Please wait before trying again.
            </p>

            <div class="countdown-timer">
                <div class="countdown-label">⏱️ Try again in:</div>
                <div class="countdown-time" id="countdown">
                    <% if (typeof retryAfter !== 'undefined' && retryAfter) { %>
                        <%= Math.ceil(retryAfter / 60000) %> minutes
                    <% } else { %>
                        15 minutes
                    <% } %>
                </div>
            </div>

            <div class="security-actions">
                <a href="/login" class="btn-security btn-primary-security">
                    <i class="ri-arrow-left-line"></i> Back to Login
                </a>
                <a href="/" class="btn-security btn-secondary-security">
                    <i class="ri-home-line"></i> Go Home
                </a>
            </div>

            <div class="developer-credit">
                <div class="developer-credit-title">
                    <i class="ri-shield-check-line"></i>
                    Secured by Xeroa CyberDefense
                </div>
                <div class="developer-credit-text">
                    Enterprise-Grade Multi-Tier Security Architecture • 4-Layer Defense Stack • Real-time Threat Detection<br>
                    Developed by <a href="https://github.com/sk-labs" target="_blank" class="developer-link">Shakir ullah (@sk-labs)</a>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Auto-refresh countdown
        <% if (typeof retryAfter !== 'undefined' && retryAfter) { %>
            let remainingSeconds = <%= Math.ceil(retryAfter / 1000) %>;

            function updateCountdown() {
                if (remainingSeconds <= 0) {
                    document.getElementById('countdown').innerHTML = '<span style="color: #28a745;">Ready! Refresh page</span>';
                    setTimeout(() => {
                        window.location.reload();
                    }, 2000);
                    return;
                }

                const minutes = Math.floor(remainingSeconds / 60);
                const seconds = remainingSeconds % 60;
                document.getElementById('countdown').textContent =
                    minutes.toString().padStart(2, '0') + ':' + seconds.toString().padStart(2, '0');

                remainingSeconds--;
            }

            setInterval(updateCountdown, 1000);
            updateCountdown();
        <% } %>
    </script>
</body>

</html>
